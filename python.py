# Store input numbers
num1 = input('Enter first value to the number: ')
num2 = input( 'Enter second value to the number: ')

# Add two numbers
sum = float(num1 + float )

# Display the sum
print('The sum of {0} and {1} is {2}'.format(num1, num2, sum))
